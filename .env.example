# Porta da API (opcional). Se vazio, usa 3101
PORT=3101

# Chave para autorizar chamadas externas (header: X-API-Key)
API_KEY=troque-esta-chave

# Raiz do lab (onde ficam catalog/, topologies/, vms/, state/, instances/)
LAB_ROOT=/opt/inoc-lab
CATALOG_DIR=/opt/inoc-lab/catalog
TOPOLOGY_DIR=/opt/inoc-lab/topologies
VM_BASE=/opt/inoc-lab/vms
STATE_DIR=/opt/inoc-lab/state
INSTANCES_DIR=/opt/inoc-lab/instances

# Backend de bridges: linux (default) ou ovs
BRIDGE_BACKEND=linux

# Range reservado de portas para instancias (opcional)
# Console: padrao 20000-29999
CONSOLE_PORT_MIN=20000
CONSOLE_PORT_MAX=29999
# Web: padrao 50000-59999
WEB_PORT_MIN=50000
WEB_PORT_MAX=59999

# Limites de memoria para reservar instancias (opcional)
# MAX_RAM: limite de RAM (padrao 95%). Aceita porcentagem.
# MAX_SWAP: limite de SWAP (padrao 0%). Aceita porcentagem.
# MIN_FREE_RAM_GB: RAM minima livre antes de reservar (padrao 2 GB).
# Exemplos:
# MAX_RAM=100%
# MAX_SWAP=95%
# MIN_FREE_RAM_GB=2

# HTTPS/TLS (opcional)
SSL_CERT=/etc/ssl/certs/inoc-lab-manager.crt
SSL_KEY=/etc/ssl/private/inoc-lab-manager.key
SSL_CA=/etc/ssl/certs/ca-bundle.crt
SSL_PASSPHRASE=

# DNS-01 via Laravel (opcional)
# LAB_DNS01_API_URL: URL base do endpoint (ex: https://dev.inocmon.com.br/api/lab-manager/dns-01)
# LAB_DNS01_API_TOKEN: token configurado no .env do Laravel (LAB_DNS01_TOKEN)
LAB_DNS01_API_URL=
LAB_DNS01_API_TOKEN=
